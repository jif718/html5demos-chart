apiVersion: v1
kind: Service
metadata:
  name: {{ include "html5demos-chart.fullname" . }}
  labels:
    {{- include "html5demos-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "html5demos-chart.selectorLabels" . | nindent 4 }}
