apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "html5demos-chart.fullname" . }}-test-connection"
  labels:
    {{- include "html5demos-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "html5demos-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
